language: python
python:
    - 3.7
install:
  - pip install codecov
  - pip install pytest-cov
  - pip install -r requirements.txt
script:
  - pytest tests --cov=uniprot_variant_import
after_success:
  - codecov